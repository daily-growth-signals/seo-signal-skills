# SignalDig Skill

本目录发布一个主要 Skill：`research-growth-signals`。

它通过一个 SignalDig MCP 连接使用两个业务工具：`research_social_signals` 与 `research_seo_signals`。

历史目录 `research-social-signals`、`research-seo-signals` 与 `decide-content-opportunities` 保留一个发布周期作为可安装兼容入口。它们统一委派到主 Skill，不维护独立请求参数。
